// input: số nguyên N: là số điểm (x, y)trên đồ thị Oxy;
//        N cặp điểm (x, y)
// output: chỉ số 2 cặp điểm có khoảng cách nhỏ nhất (chỉ có duy nhất 2 điểm gần nhất, các điểm không trùng nhau)

#include <iostream>

using namespace std;

int main()
{

    ios_base::sync_with_stdio(false);
    cin.tie(0);
}